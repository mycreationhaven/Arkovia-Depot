(()=>{const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=v=>v===null||v===undefined?'—':Number(v).toLocaleString(undefined,{maximumFractionDigits:8});async function load(){const count=$('livePlayerCount'),list=$('livePlayerList');if(!count||!list)return;try{const r=await fetch('/api/players',{credentials:'same-origin'});if(!r.ok)throw new Error('unavailable');const d=await r.json();const players=d.players||d.Players||[];count.textContent=String(d.count??d.Count??players.length);list.innerHTML=players.length?players.map(p=>{const name=p.name??p.Name??'Adventurer',loc=p.location??p.Location??'Arkovia',game=p.gameBalance??p.GameBalance??0,chain=p.chainBalance??p.ChainBalance,currency=p.currency??p.Currency??'ARKOS',wallet=p.walletAddress??p.WalletAddress;return `<article class="live-player"><div class="player-name-row"><span class="online-dot"></span><strong>${esc(name)}</strong></div><small>${esc(loc)}</small><div class="player-balances"><span><b>${fmt(game)}</b><small>In-game ${esc(currency)}</small></span><span><b>${fmt(chain)}</b><small>On-chain ${esc(currency)}</small></span></div>${wallet?`<div class="wallet-mini" title="${esc(wallet)}">${esc(wallet.slice(0,10))}…${esc(wallet.slice(-6))}</div>`:'<div class="wallet-mini muted">No linked ARK wallet</div>'}</article>`}).join(''):'<div class="community-empty">No adventurers are online right now.</div>';}catch{count.textContent='—';list.innerHTML='<div class="community-empty">Live player balances are temporarily unavailable.</div>';}}document.addEventListener('DOMContentLoaded',()=>{load();setInterval(load,30000);});})();
